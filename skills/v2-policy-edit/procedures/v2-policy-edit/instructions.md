# v2_policy_edit

Load the generated knowledge, evaluate restrictions, collect required facts, require confirmation, and use only the declared tool.

## Exit

Finish only after the requested task is completed, blocked with a clear reason, or waiting for required input.
